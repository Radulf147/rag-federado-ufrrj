# Projeto: Agente RAG Federado — UFRRJ
# Dependências Python

# Framework RAG
haystack-ai==2.9.0

# Integrações Haystack
chroma-haystack==2.0.0
ollama-haystack==2.2.0

# ChromaDB (cliente + servidor embutido)
chromadb==0.6.3

# Embeddings
sentence-transformers==3.4.1

# Scraping
httpx==0.27.2
beautifulsoup4==4.12.3
lxml==5.3.0

# NLP para chunking por sentença (DocumentSplitter)
nltk==3.9.1

rapidfuzz==3.9.3
# Validação de schema das tools (haystack.tools.Tool importa jsonschema)
jsonschema==4.23.0

# Suite de testes (armadilha 2 do CLAUDE.md). Ate 5 set 2026 nao existia
# nenhum test_*.py, e rodar pytest coletava zero testes saindo com sucesso —
# que se le facilmente como "esta tudo passando".
pytest==8.3.4
